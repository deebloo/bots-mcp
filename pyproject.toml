[project]
name = "bots-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["fastmcp>=3.1.1", "httpx>=0.27"]

[project.scripts]
bots-mcp = "main:mcp.run"
